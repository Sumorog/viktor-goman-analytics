# План подготовки

1. Оконные функции (`ROW_NUMBER`, `AVG() OVER`) — см. задачи 1–2 в `interview_tasks.sql`.
2. JOIN + группировка — связка с реальными запросами [01-sql-tariffs/query_revenue_by_resident_segment.sql](../01-sql-tariffs/query_revenue_by_resident_segment.sql).
3. FULL OUTER JOIN для сверок — см. `query_accruals_reconciliation.sql` в 01.
