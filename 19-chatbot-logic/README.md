# 19 — Логика ИИ-ассистента (запись на услугу)

JSON-дерево: намерения пользователя, слоты, переходы и fallback при неоднозначности.

## Файлы

- `bot_logic.json` — конфигурация сценария (упрощённо, без привязки к конкретному движку диалогов).
